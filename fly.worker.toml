app = "octodrop-platform-worker"
primary_region = "syd"

[build]
  dockerfile = "Dockerfile"
  [build.args]
    OCTO_RUNTIME_BASE = "registry.fly.io/octodrop-platform-api:runtime-base-py311-playwright"

[processes]
  app = "python -m app.worker"
