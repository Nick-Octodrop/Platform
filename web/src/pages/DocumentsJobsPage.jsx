import React from "react";
import SettingsPlaceholder from "../ui/SettingsPlaceholder.jsx";

export default function DocumentsJobsPage() {
  return (
    <SettingsPlaceholder
      title="Render Jobs"
      description="Queued, running, and failed renders."
    />
  );
}
